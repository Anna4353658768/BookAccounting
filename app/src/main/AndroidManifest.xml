<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        android:parentActivityName=".BookListActivity">

        <activity
            android:name=".activity.pager.BookPagerActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.pager.AuthorPagerActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.pager.PublisherPagerActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.list.BookListActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.list.PublisherListActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.list.AuthorListActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".activity.AuthorizationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.RegistrationActivity"
            android:exported="true">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>